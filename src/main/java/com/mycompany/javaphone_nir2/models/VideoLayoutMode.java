package com.mycompany.javaphone_nir2.models;

/**
 * Camera modes in video call
 */
public enum VideoLayoutMode {
    PIP_PRIMARY_FIRST, // primary full, remote small
    PIP_PRIMARY_SECOND, // remote full, primary small
    SPLIT                // each camera same size
}
